defaults:
  - dynamic_property: dp01
  - dynamic_state: ds01
  - static: s01

data_to_get:
  - waypoints

delta_time: 0.1  # difference in time of 2 consecutive datapoint (=1/frequency)
duration: 20.  # duration of a data scene (seconds)
# for sub-scene